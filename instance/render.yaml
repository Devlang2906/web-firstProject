services:
  - type: web
    name: unpas-trade
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: unpas-db
          property: connectionString

databases:
  - name: unpas-db
    plan: free